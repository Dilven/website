import { Della_Respira, Oswald } from 'next/font/google';

export const mainFont = Oswald({ subsets: ['latin'], weight: ['400', '700'] });
export const logoFont = Della_Respira({ subsets: ['latin'], weight: ['400'] });
